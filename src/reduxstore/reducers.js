var demo = function (state, action) {
    switch (action.type) {
        case "LOGIN": {
            console.log("Here we have to write");
        }

        case "LOGOUT": {
            console.log("Here we have to write");
        }
    }
};
